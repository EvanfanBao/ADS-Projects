There is a single cpp file in this directory which contains our source code to implement skip list. Also, there is an exe file which can be executed directly on Windows. Following the hint of command line, after entering skip list size and times of execution, you can get the time result. If you want to see the data details of result of skip list, use the PrintList function in cpp file, we comment it now in cpp because the output may be too long.
